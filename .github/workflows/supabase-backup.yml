name: Supabase Weekly Backup
on:
  schedule:
    - cron: '0 3 * * 0'
  workflow_dispatch: {}

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Install Supabase CLI
        run: |
          curl -sL https://github.com/supabase/cli/releases/latest/download/supabase_linux_amd64.tar.gz | tar xz
          sudo mv supabase /usr/local/bin/

      - name: Dump critical tables
        env:
          DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          mkdir -p backup
          pg_dump "postgresql://postgres.mzmskujwpqmokbrdbwbh:${DB_PASSWORD}@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres" \
            --data-only \
            -t public.devices -t public.device_channels -t public.device_members \
            -t public.device_events \
            -f backup/a5x-home-backup-$(date +%F).sql

      - name: Upload as artifact
        uses: actions/upload-artifact@v4
        with:
          name: supabase-backup-${{ github.run_id }}
          path: backup/*.sql
          retention-days: 90
